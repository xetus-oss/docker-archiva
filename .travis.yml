services:
 - docker

install: true

script:
  - make test

#
# Push from the docker-ci system after success
#
deploy:
  provider: script
  script:
    - ./travis-ci-push.sh
  on:
    all_branches: true
    tags: true